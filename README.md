# CareFlow – Consultation Assistant API

## Overview
Base setup for the Medassist (Laravel + Next.js + Docker + MySQL).
This branch contains the core environment configuration used by all feature branches.

## Quick Start
```bash
docker-compose up --build
